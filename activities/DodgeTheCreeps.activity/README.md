This is an experimental embedded a Godot game sugarizer.

To build the activity update the game submodule where the game code is stored,
the run `build.sh` from inside the DodgeTheCreeps.activity folder.